import { AppBar, Toolbar } from "@mui/material"

const Navbar = ()=>{
    return(
        <AppBar position='static'>
            <Toolbar>
                Hello
            </Toolbar>
        </AppBar>
    )
}

export default Navbar;